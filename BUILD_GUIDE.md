# دليل بناء وتثبيت تطبيق كاشف الإيبان

## 🎯 الهدف
هذا الدليل يشرح كيفية بناء ملف APK وتثبيت التطبيق على هاتفك الأندرويد.

---

## 📋 المتطلبات الأساسية

### 1. تثبيت Android Studio
قم بتحميل وتثبيت Android Studio من الموقع الرسمي:
- 🔗 https://developer.android.com/studio

### 2. تثبيت JDK 17
- Android Studio يأتي مع JDK مدمج
- أو قم بتحميله من: https://adoptium.net/

### 3. Android SDK
Android Studio يقوم بتثبيته تلقائياً، تأكد من تثبيت:
- ✅ Android SDK Platform 34
- ✅ Android SDK Build-Tools 34.0.0
- ✅ Android SDK Command-line Tools

---

## 🚀 طريقة 1: البناء باستخدام Android Studio (الأسهل)

### الخطوات:

#### 1. فتح المشروع
```
1. افتح Android Studio
2. اختر "Open an Existing Project"
3. اختر مجلد IbanDetectorApp
4. انتظر حتى يتم تحميل المشروع وتنزيل التبعيات
```

#### 2. مزامنة Gradle
```
1. من القائمة: File → Sync Project with Gradle Files
2. انتظر حتى تكتمل المزامنة (قد يستغرق 2-5 دقائق في أول مرة)
```

#### 3. بناء APK
```
1. من القائمة: Build → Build Bundle(s) / APK(s) → Build APK(s)
2. انتظر حتى تظهر رسالة "APK(s) generated successfully"
3. اضغط على "locate" لفتح مجلد APK
```

#### 4. موقع الملف
```
المسار: IbanDetectorApp/app/build/outputs/apk/debug/app-debug.apk
```

---

## ⚡ طريقة 2: البناء من سطر الأوامر (للمتقدمين)

### على Windows:

#### 1. افتح Command Prompt
```cmd
cd C:\path\to\IbanDetectorApp
```

#### 2. بناء APK
```cmd
gradlew.bat assembleDebug
```

#### 3. موقع الملف
```
app\build\outputs\apk\debug\app-debug.apk
```

### على macOS/Linux:

#### 1. افتح Terminal
```bash
cd /path/to/IbanDetectorApp
```

#### 2. إعطاء صلاحيات التنفيذ
```bash
chmod +x gradlew
chmod +x build.sh
```

#### 3. بناء APK (خيار 1 - باستخدام السكريبت)
```bash
./build.sh
```

#### 4. بناء APK (خيار 2 - مباشرة)
```bash
./gradlew assembleDebug
```

#### 5. موقع الملف
```
app/build/outputs/apk/debug/app-debug.apk
```

---

## 📱 تثبيت APK على الهاتف

### طريقة 1: نقل الملف مباشرة

#### الخطوات:
```
1. انقل ملف app-debug.apk إلى هاتفك (عبر USB أو البريد الإلكتروني أو Google Drive)
2. افتح مدير الملفات على الهاتف
3. ابحث عن ملف app-debug.apk
4. اضغط عليه
5. إذا ظهرت رسالة "تثبيت من مصادر غير معروفة":
   - اذهب إلى الإعدادات
   - فعّل "السماح من هذا المصدر"
6. اضغط على "تثبيت"
7. انتظر حتى يكتمل التثبيت
8. اضغط على "فتح" أو ابحث عن أيقونة "كاشف الإيبان" في التطبيقات
```

### طريقة 2: استخدام ADB (للمتقدمين)

#### التحضير:
```
1. فعّل "وضع المطور" على هاتفك:
   - الإعدادات → حول الهاتف
   - اضغط 7 مرات على "رقم البناء"
   
2. فعّل "USB Debugging":
   - الإعدادات → خيارات المطور
   - فعّل "تصحيح أخطاء USB"
   
3. وصّل هاتفك بالكمبيوتر عبر USB
4. اقبل رسالة "السماح بتصحيح الأخطاء"
```

#### التثبيت:
```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

#### في حالة وجود نسخة قديمة:
```bash
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### طريقة 3: التثبيت مباشرة من Android Studio

```
1. وصّل هاتفك بالكمبيوتر
2. فعّل USB Debugging
3. في Android Studio، اختر هاتفك من القائمة المنسدلة
4. اضغط على زر "Run" (▶️) أو Shift+F10
5. سيتم تثبيت التطبيق تلقائياً
```

---

## 🏗️ بناء نسخة الإصدار (Release) للنشر

### 1. إنشاء Keystore

```bash
keytool -genkey -v -keystore iban-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias iban-key
```

### 2. تعديل app/build.gradle.kts

أضف في قسم android:

```kotlin
signingConfigs {
    create("release") {
        storeFile = file("../iban-release-key.jks")
        storePassword = "your-keystore-password"
        keyAlias = "iban-key"
        keyPassword = "your-key-password"
    }
}

buildTypes {
    getByName("release") {
        signingConfig = signingConfigs.getByName("release")
        // ...
    }
}
```

### 3. بناء Release APK

```bash
./gradlew assembleRelease
```

### 4. موقع الملف

```
app/build/outputs/apk/release/app-release.apk
```

---

## 🔧 حل المشاكل الشائعة

### مشكلة: "SDK location not found"
**الحل:**
```
1. افتح ملف local.properties
2. أضف: sdk.dir=/path/to/android/sdk
3. مثال (Windows): sdk.dir=C\:\\Users\\YourName\\AppData\\Local\\Android\\Sdk
4. مثال (Mac): sdk.dir=/Users/YourName/Library/Android/sdk
```

### مشكلة: "Gradle sync failed"
**الحل:**
```
1. File → Invalidate Caches → Invalidate and Restart
2. انتظر إعادة التشغيل
3. جرب المزامنة مرة أخرى
```

### مشكلة: "Build failed - Out of memory"
**الحل:**
```
1. افتح ملف gradle.properties
2. أضف أو عدّل: org.gradle.jvmargs=-Xmx4096m
3. احفظ وأعد المحاولة
```

### مشكلة: "App not installed"
**الحل:**
```
1. امسح التطبيق القديم إذا كان موجوداً
2. تأكد من السماح بالتثبيت من مصادر غير معروفة
3. تأكد من أن التوقيع الرقمي متطابق
```

### مشكلة: لا تظهر أجهزة في Android Studio
**الحل:**
```
1. تأكد من تفعيل USB Debugging
2. جرب كابل USB آخر
3. جرب منفذ USB آخر
4. ثبت Google USB Driver (Windows)
5. في Terminal: adb devices
```

---

## 📊 حجم APK المتوقع

- **Debug APK**: ~15-25 MB
- **Release APK**: ~10-18 MB (بعد الضغط والتحسين)

---

## ⚠️ ملاحظات مهمة

1. ✅ **أول بناء**: قد يستغرق 5-10 دقائق لتنزيل التبعيات
2. ✅ **الأذونات**: التطبيق يطلب إذن الكاميرا فقط
3. ✅ **الأمان**: التطبيق لا يرسل أي بيانات لخوادم خارجية
4. ✅ **التخزين**: جميع البيانات محفوظة محلياً على الجهاز
5. ✅ **الحد الأدنى**: Android 7.0 (API 24) أو أحدث

---

## 🎓 موارد إضافية

- 📖 [وثائق Android Developer](https://developer.android.com/docs)
- 📖 [دليل Gradle](https://docs.gradle.org/)
- 📖 [معلومات عن IBAN](https://ar.wikipedia.org/wiki/IBAN)

---

## 💡 نصائح للمطورين

### تفعيل التوقيع التلقائي للتطوير

قم بإضافة في `app/build.gradle.kts`:

```kotlin
signingConfigs {
    getByName("debug") {
        storeFile = file("debug.keystore")
        storePassword = "android"
        keyAlias = "androiddebugkey"
        keyPassword = "android"
    }
}
```

### تسريع البناء

في `gradle.properties`:

```properties
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true
kotlin.incremental=true
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تحقق من قسم "حل المشاكل الشائعة" أعلاه
2. راجع ملف README.md الرئيسي
3. تحقق من ملفات log في Android Studio
4. ابحث عن الخطأ في Google أو Stack Overflow

---

**تم إنشاء هذا الدليل بواسطة MiniMax Agent**

**الإصدار**: 1.0.0  
**التاريخ**: نوفمبر 2025
